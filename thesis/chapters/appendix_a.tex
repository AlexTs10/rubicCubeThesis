\chapter{Οδηγός Εγκατάστασης και Χρήσης}
\label{app:usage}

\section{Απαιτήσεις Συστήματος}

Για την εκτέλεση του project απαιτείται:
\begin{itemize}
    \item Python 3.12--3.14 (συνιστώμενη baseline έκδοση: Python 3.12)
    \item \texttt{pip} για εγκατάσταση εξαρτήσεων
    \item βασικές βιβλιοθήκες επιστημονικού Python stack:
    \texttt{numpy}, \texttt{scipy}, \texttt{pandas}, \texttt{matplotlib}
    \item προαιρετικά: \texttt{streamlit} για το Python UI
    \item προαιρετικά: \texttt{node/npm} για τη Next.js εφαρμογή
\end{itemize}

Η τρέχουσα έκδοση του αποθετηρίου υποστηρίζει \texttt{Python 3.12} έως \texttt{Python 3.14}, με συνιστώμενη baseline έκδοση την \texttt{Python 3.12}. Για πλήρη έλεγχο του περιβάλλοντος διατίθεται το script \filepath{verify_setup.py}.

\section{Εγκατάσταση}

\subsection{Βασική Εγκατάσταση}

\begin{lstlisting}
python3.12 -m venv .venv
source .venv/bin/activate
python -m pip install --upgrade pip
python -m pip install --require-hashes -r requirements.lock
python -m pip install --no-deps -e .
\end{lstlisting}

\subsection{Έλεγχος Περιβάλλοντος}

\begin{lstlisting}
python verify_setup.py
\end{lstlisting}

\subsection{Εκτέλεση Test Suite}

Το ακόλουθο command εκτελεί το προεπιλεγμένο γρήγορο προφίλ αναπαραγωγιμότητας.
Οι αργές δοκιμές solver-quality, τα external backends και οι cache-building
δοκιμές είναι opt-in marker profiles.

\begin{lstlisting}
python -m pytest tests -q
\end{lstlisting}

\begin{lstlisting}
python -m pytest tests -q -m slow
python -m pytest tests -q -m external
python -m pytest tests -q -m cache_building
\end{lstlisting}

Το \texttt{external} profile συλλέγει τις δοκιμές εξωτερικών backends, αλλά οι
oracle-dependent έλεγχοι παραμένουν προστατευμένοι με environment gate:
\texttt{RUN\_NATIVE\_ORACLE\_TESTS=1} και εγκατεστημένο \texttt{RubikOptimal}.

\section{Βασική Χρήση}

Η απλούστερη χρήση του project είναι μέσω των demo scripts. Τα scripts αυτά καλύπτουν δημιουργία κύβου, scrambling, επίλυση και σύγκριση αλγορίθμων.

\subsection{Εκτέλεση Demos}
\begin{lstlisting}
python demos/basic_usage.py
python demos/kociemba_demo.py
python demos/thistlethwaite_demo.py
python demos/a_star_comparison_demo.py
python demos/distance_estimator_demo.py
\end{lstlisting}

\subsection{Benchmarks και Συγκρίσεις}

\begin{lstlisting}
python demos/phase9/benchmark_demo.py --n-scrambles 20
python demos/phase9/algorithm_comparison_cli.py --depth 10 --seed 42
\end{lstlisting}

\section{Διαδραστικές Εφαρμογές}

\subsection{Streamlit}

\begin{lstlisting}
streamlit run ui/app.py
\end{lstlisting}

\subsection{Next.js Web Application}

\begin{lstlisting}
cd webapp
npm ci
npm run build
npm run dev
\end{lstlisting}

Η εφαρμογή Next.js λειτουργεί ως presentation/demo frontend και όχι ως authoritative live διεπαφή εκτέλεσης των Python solvers. Παρέχει σελίδες για:
\begin{itemize}
    \item αρχική παρουσίαση του project,
    \item demo επίλυσης μεμονωμένου scramble,
    \item demo σύγκρισης αλγορίθμων,
    \item και εκπαιδευτικό περιεχόμενο.
\end{itemize}
Η ζωντανή εκτέλεση των αλγορίθμων γίνεται από το \filepath{ui/} και από τα scripts benchmark/validation του repository.

\section{Αναπαραγωγή Αποτελεσμάτων Διπλωματικής}

Για την ακριβή αναπαραγωγή των αποτελεσμάτων του Κεφαλαίου~\ref{ch:evaluation}
χρησιμοποιείται ως επίσημη διαδρομή το script
\filepath{scripts/benchmarks/regenerate_thesis_benchmarks.py}, το οποίο
επαναεκτελεί τα benchmarks πάνω στο αποθηκευμένο σύνολο scrambles και
αναδημιουργεί τα αρχεία
\filepath{results/benchmarks/thesis/thesis_bench_d*.json} και
\filepath{results/benchmarks/thesis/thesis_results_combined.json}. Προαιρετικά
μπορούν να εκτελεστούν και τα scripts ανάλυσης, τα οποία διαβάζουν τα παραπάνω
αρχεία από τον ίδιο κατάλογο:

\begin{lstlisting}
python scripts/benchmarks/regenerate_thesis_benchmarks.py
python scripts/benchmarks/analyze_thesis_data.py
python scripts/benchmarks/generate_latex_tables.py
python -m src.evaluation.visualizations results/benchmarks/thesis/thesis_results_combined.json thesis/figures
\end{lstlisting}

Τα scripts \filepath{generate_thesis_data.py} και
\filepath{generate_complete_thesis_data.py} διατηρούνται μόνο για ιστορικούς ή
διερευνητικούς λόγους και δεν αποτελούν τη διαδρομή αναπαραγωγής της
διπλωματικής.

Για τη διαχείριση της συγγραφής και των chapter packets προστέθηκε επίσης workflow script:

\begin{lstlisting}
python scripts/thesis_workflow.py status
python scripts/thesis_workflow.py packets --remaining
python scripts/thesis_workflow.py validate
\end{lstlisting}

\section{Στιγμιότυπο Αναπαραγωγιμότητας}
\label{sec:reproducibility_snapshot}

Για τη συγκεκριμένη έκδοση της διπλωματικής, τα παρακάτω στοιχεία αποτυπώνουν την ακριβή κατάσταση κώδικα, εξαρτήσεων και benchmark artifacts. Όλες οι αναφορές αριθμητικών αποτελεσμάτων στα Κεφάλαια \ref{ch:evaluation} και \ref{ch:implementation} αντιστοιχούν σε αυτό το snapshot.

\begin{table}[htbp]
\centering
\caption{Στοιχεία αναπαραγωγιμότητας της διπλωματικής}
\label{tab:reproducibility_snapshot}
\begin{tabularx}{\textwidth}{>{\RaggedRight\arraybackslash}p{4.4cm} Y}
\toprule
\textbf{Πεδίο} & \textbf{Τιμή} \\
\midrule
Archive manifest & \texttt{REPRODUCIBILITY\_MANIFEST.json} \\
Revision source & ZIP manifest, not \texttt{.git} metadata \\
Repository reference & \texttt{AlexTs10/rubicCubeThesis} \\
Python & 3.12.2 \\
Πλατφόρμα & Apple M3, 16~GB RAM, macOS 26.2 \\
Test suite & Fast profile excludes tests marked \texttt{slow}, \texttt{external}, or \texttt{cache\_building}; exact collected counts are regenerated by \texttt{pytest --collect-only} \\
\filepath{verify\_setup.py} & Fast reproducibility profile by default; full profile opt-in \\
Benchmark timestamp & 2026-03-24T01:28:24 \\
Benchmark corpus & 100 scrambles (25 ανά βάθος 5/10/15/20) \\
Kociemba backend & \texttt{kociemba\_internal} (100/100, χωρίς fallback) \\
Thistlethwaite backend & \texttt{thistlethwaite\_native} (100/100) \\
Korf benchmark backend & \texttt{optimal\_external} \\
Εξωτερικός optimal backend & \texttt{RubikOptimal 1.1.0} (\texttt{hkociemba/RubiksCube-OptimalSolver}) \\
\texttt{source-zip} native validation & 261 cases, no generated corner DB and no external oracle required \\
\texttt{canonical} native validation & 3,513 cases; requires \texttt{data/pattern\_databases/corner\_db.pkl} and the external exact oracle \\
\bottomrule
\end{tabularx}
\end{table}

Τα αρχεία αλήθειας του snapshot είναι:
\begin{itemize}
    \item \filepath{results/benchmarks/thesis/thesis\_results\_combined.json}
    \item \filepath{results/benchmarks/thesis/thesis\_bench\_d5.json},
    \filepath{results/benchmarks/thesis/thesis\_bench\_d10.json},
    \filepath{results/benchmarks/thesis/thesis\_bench\_d15.json},
    \filepath{results/benchmarks/thesis/thesis\_bench\_d20.json}
    \item \filepath{thesis/main.tex} και τα κεφάλαια στο \filepath{thesis/chapters/}.
\end{itemize}

\section{Σημειώσεις}

\begin{itemize}
    \item Η πρώτη εκτέλεση ορισμένων solvers μπορεί να είναι αισθητά πιο αργή λόγω δημιουργίας ή φόρτωσης πινάκων.
    \item Τα benchmark scripts αποθηκεύουν ρητά τα scrambles και τα αποτελέσματα σε JSON μορφή.
    \item Η πλήρης παραγωγή PDF της διπλωματικής εκτελείται με \texttt{python scripts/thesis\_workflow.py build --mode auto}, το οποίο δοκιμάζει κατά σειρά \texttt{latexmk}/XeLaTeX, χειροκίνητο XeLaTeX με βιβλιογραφία, \texttt{tectonic}, και τέλος Docker όταν υπάρχει ενεργό container runtime.
\end{itemize}
