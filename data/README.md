# Data Directory

This directory contains generated caches and lookup tables used by the solver implementations.

## Current Structure

```text
data/
├── kociemba/
│   ├── move_tables/
│   └── pruning_tables/
├── move_tables/
├── pattern_databases/
│   ├── corner_db.pkl
│   ├── edge1_db.pkl
│   ├── edge2_db.pkl
│   ├── native_exact/
│   └── native_exact_dev/
└── pruning_tables/
```

## What Lives Here

### `data/kociemba/`

Cached move/pruning tables for the internal Kociemba two-phase solver in `src/kociemba/`.

### `data/pattern_databases/`

Pattern-database artifacts used by:

- the exploratory Korf/IDA* code path
- the native exact solver heuristics
- the native corner pattern database infrastructure and generated cache files

This includes both large thesis-grade caches such as `corner_db.pkl` and smaller coordinate tables under `native_exact/`. Some of these caches are generated on demand and may need to be regenerated in a fresh checkout.

### `data/move_tables/` and `data/pruning_tables/`

Legacy/shared caches created by earlier solver generations. They are still used by parts of the codebase and should be treated as generated artifacts, not handwritten source files.

## Notes

- The canonical thesis benchmark corpus is not stored here. It lives under `results/benchmarks/thesis/`.
- Validation reports are also not stored here. They live under `results/validation/`.
- Large cache files may be regenerated by the relevant solver or benchmark scripts if missing.
- Some files are versioned because they are part of the verified thesis workflow.
